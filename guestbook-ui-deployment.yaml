apiVersion: apps/v1
kind: Deployment
metadata:
  name: shark-app
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: shark-app
  template:
    metadata:
      labels:
        app: shark-app
    spec:
      containers:
      - image: nginx
        name: shark-app
